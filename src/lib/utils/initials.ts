// LIVE TDD DEMO - the helper you build on stage. Committed COMMENTED OUT so the
// public repo and prod ship no real feature. During the demo, uncomment the
// export below (Step 2: GREEN) to make the test - and the header avatar badge -
// pass. See docs/live-tdd-feature.md.
//
// /** Up to two uppercase initials from a full name ("Test Admin" -> "TA"). */
// export function initials(name: string): string {
// 	return name
// 		.trim()
// 		.split(/\s+/)
// 		.slice(0, 2)
// 		.map((w) => w[0] ?? '')
// 		.join('')
// 		.toUpperCase();
// }

export {};
