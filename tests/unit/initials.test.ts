import { describe, it } from 'vitest';
// import { expect } from 'vitest';
// import { initials } from '../../src/lib/utils/initials';

// LIVE TDD DEMO - committed as a placeholder so the public repo / CI carry no
// real feature test. On stage: uncomment the imports above and the assertions
// below (Step 1: RED), then uncomment the util export in
// src/lib/utils/initials.ts (Step 2: GREEN). The it.todo keeps Vitest green in
// the meantime - a test file with zero collected tests fails the suite.
describe('initials', () => {
	it.todo('takes up to two uppercase initials from a name');
	// it('takes up to two uppercase initials from a name', () => {
	// 	expect(initials('Test Admin')).toBe('TA');
	// 	expect(initials('Pathfinder')).toBe('P');
	// 	expect(initials('  jane   doe ')).toBe('JD');
	// });
});
